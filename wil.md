react는 기본적으로 javascript를 기반으로 함

**web은 크게 HTML + CSS + Javascript의 구성으로 볼 수 있음**
HTML(HyperText Markup Language) : 브라우저에게 “무엇이 있는지”(제목, 글, 이미지, 버튼 등)을 알려주는 역할
CSS(Cascading Style Sheets) : 웹페이지에 “어떻게 보일지”(색상, 크기, 간격 등등) 적는 언어
Javascript : 웹페이지의 변화와 동작을 만드는 역할, Javascript는 DOM(Document Object Model : 브라우저가 HTML 문서를 읽고 각 요소(태그, 텍스트, 속성 등)를 트리 구조의 객체로 만든 것)을 통해 웹페이지의 요소를 찾고 바꿀 수 있음
event : 사용자의 입력(마우스 클릭, 키보드 입력, 화면 터치)이나 상태 변화 등 ‘무엇인가 일어났음’을 알리는 신호나 메시지
**주 원리 : 사람의 행동 → event → javascript → 화면의 변화**